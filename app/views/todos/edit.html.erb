<div class="wrapper">
    <div class="card">
        <div class="card-header bg-primary text-white font-weight-bold">TODO LISTs - EDIT</div>
        <%= render 'form', todo: @todo %>
        <div class="card-footer text-right">
            <%= link_to 'Back', todos_path, class: 'btn btn-sm btn-primary' %>
        </div>
    </div>
    <%= render 'credit' %>
</div>
